"""Console entry points for expense-agent-svc.

The trajectory + cost + optional RAGAS gate lives in
:mod:`expense_agent_svc.scripts.eval` and runs against the 20
committed scenarios in ``evals/scenarios.jsonl``.
"""
